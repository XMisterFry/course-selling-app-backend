let {Router} = require ("express");
const {coursesModel} = require ("../db")
let courseRouter = Router();





courseRouter.post ('/purchase', (req,res)=> {
    
})

courseRouter.get ('/view', (req,res)=> {
    
})

module.exports = {courseRouter}